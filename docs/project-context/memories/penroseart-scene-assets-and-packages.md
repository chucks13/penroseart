<!--
Generated by /project export
Source Memory Vault ID: penroseart-scene-assets-and-packages
Memory Vault remains canonical.
-->

# PenroseArt Scene, Assets, and Packages

## Active scene

`Assets/Scenes/SampleScene.unity` is the only enabled build scene in `ProjectSettings/EditorBuildSettings.asset`.

Custom MonoBehaviour bindings found in the scene:

| Scene object     | Script                                | Notes                                                                                             |
| ---------------- | ------------------------------------- | ------------------------------------------------------------------------------------------------- |
| `Controller`     | `Assets/core/Runtime/Controller.cs`           | Main runtime hub. Script meta GUID `5f47cb3b2738dba41858c13614a36b80`; execution order `-2000`.   |
| `PenroseDisplay` | `Assets/core/Runtime/Penrose.cs`              | Mesh display and Penrose wall data/model. Script meta GUID `f95c9fce4577c274998e22a24e80df9a`.    |
| `Slider`         | `Assets/core/helpers/SliderScript.cs` | UI brightness slider. Script meta GUID `05b21a700386d4945873bb439c1c8669`; slider range is 0-255. |

Other scene objects are Unity/TMP/UI primitives such as `Main Camera`, `GUI Canvas`, `EventSystem`, `DebugText`, `EffectText`, `destIP`, `ontime`, `offtime`, display toggle, slider children, and labels.

## Important serialized Controller values

Investigation found these values in `SampleScene.unity`:

- `IP: 192.168.1.253`
- `brightness: 64`
- `displayOn: 1`
- `useCamera: 0`
- `effectTime: 10`
- `currentTransition: 4`
- `transitionTime: 4`
- `paletteSource`: large FastLED-style palette string serialized into the scene.
- `jsonSource`: large JSON wall map serialized into the scene.
- `effectText`, `debugText`, `myIPText`: serialized TMP text references.
- Serialized `penrose` reference is `{fileID: 0}`; `Controller.Start()` finds Penrose dynamically with `GameObject.FindObjectOfType<Penrose>()`.

The scene `jsonSource` was locally verified by the context-builder to exactly match `Assets/StreamingAssets/rawdata.json` at investigation time.

## StreamingAssets

`Assets/StreamingAssets/` contains runtime data:

- `rawdata.json` — 244,490 bytes; parsed counts: 900 tiles, 1,800 wires, 10,800 mesh floats, shape lists including `lines0..4`, `loops`, `lotusballs`, `starballs`, `stars`, `mirror10`, `mirror2`.
- `rawdata1.json` — alternate/formatted Penrose data with same major counts, but `loops` count differs by one entry.
- `palettedata.txt`, `jenpalettes.txt`, `filelist.txt` — palette/list data.
- `images/color/*.png`, `images/mono/*.png` — image sources for `kscope`.
- `images/color/files.txt` and `images/mono/files.txt` — file lists that `kscope` can regenerate on non-Android.

## Project settings

Key settings found:

- Unity editor: `6000.4.7f1 (f3c3c4248748)`.
- Product: company `Chuck`, product `penrose_simulator`.
- Android package id: `com.hunter.penrosesimulator`.
- Android minimum SDK: `23`.
- Android target architecture field value: `5`.
- Default screen size: 1024x768.
- Bundle version: `0.1`.
- Built-in Render Pipeline: `ProjectSettings/GraphicsSettings.asset` has no custom render pipeline asset.
- Gamma color space: `m_ActiveColorSpace: 0`.
- Project scripting define symbols are empty in `ProjectSettings/ProjectSettings.asset`; active serial output is controlled by a file-local define in `Controller.cs`.
- Text serialization is enabled in `ProjectSettings/EditorSettings.asset` (`m_SerializationMode: 2`).
- Standalone API compatibility is currently `.NET Standard 2.1` via per-platform override (`apiCompatibilityLevelPerPlatform: Standalone: 6`). Desktop `System.IO.Ports` support is supplied by platform-specific plugin assets under `Assets/Plugins/System.IO.Ports/`.

## Package dependencies

Direct dependencies in `Packages/manifest.json` include:

- `com.unity.ai.navigation` `2.0.12`
- `com.unity.collab-proxy` `2.12.4`
- `com.unity.ext.nunit` `2.0.5`
- `com.unity.ide.visualstudio` `2.0.27` is the current editor integration package; deprecated `com.unity.ide.vscode` was removed.
- `com.unity.multiplayer.center` `1.0.1`
- `com.unity.test-framework` `1.6.0`
- `com.unity.timeline` `1.8.12`
- `com.unity.ugui` `2.0.0`
- `com.unity.ui.builder` `2.0.0`
- many built-in Unity modules, including UI, UIElements, UnityWebRequest, image conversion, audio, physics/physics2d, tilemap, video, XR, and others.

`Packages/packages-lock.json` exists and should remain under source control for deterministic package resolution.

## Generated IDE/project files

- `Assembly-CSharp.csproj` is Unity-generated and should not be hand-edited. After API compatibility changes it can remain stale until Unity regenerates it; treat `ProjectSettings/ProjectSettings.asset` as the source of truth for API compatibility.
- `penroseart.slnx` is Unity-generated and points at `Assembly-CSharp.csproj`.
- `.vscode/settings.json` sets `dotnet.defaultSolution` to `penroseart.slnx` and hides many Unity/generated asset types.
- `.vscode/extensions.json` recommends `visualstudiotoolsforunity.vstuc`.
- `.vscode/launch.json` defines `Attach to Unity` using `vstuc`.
- `.vsconfig` requests `Microsoft.VisualStudio.Workload.ManagedGame`.

## Asset hygiene notes

- TextMesh Pro resource/example changes and new generated Unity settings/packages were committed as part of the UnityUpgrade import/config state. Treat future changes in these areas as user/Unity-owned unless explicitly confirmed otherwise.
- Preserve `.meta` files and GUIDs when moving or renaming assets.
