<!--
Generated by /project export
Source Memory Vault ID: penroseart-gitignore-assessment
Memory Vault remains canonical.
-->

# PenroseArt `.gitignore` Assessment

Current `.gitignore` is a small Unity ignore file. It was updated during the UnityUpgrade cleanup to fix the highest-impact broken patterns.

## Working entries

- `/[Ll]ibrary/`, `/[Tt]emp/`, `/[Oo]bj/`, and `/[Bb]uild/` correctly ignore common Unity/generated directories.
- `*.csproj`, `*.unityproj`, `*.sln`, `*.suo`, `*.user`, `*.userprefs`, and `*.pidb` ignore generated IDE/project files.
- `*.meta` plus `!/[Aa]ssets/**/*.meta` keeps Unity asset GUID metadata under `Assets/` while ignoring non-asset `.meta` files.
- `.vs/` and `*.dotsettings` are present, but existing `.vs/` files are already tracked so the ignore does not remove them.

## Fixed entries

- `/[Logs]/`, `/[MemoryCaptures]/`, and `/[UserSettings]/` were replaced with correct case-insensitive directory patterns: `/[Ll]ogs/`, `/[Mm]emoryCaptures/`, and `/[Uu]ser[Ss]ettings/`.
- `*.slnx` was added so Unity-generated `penroseart.slnx` stays ignored.
- `*.log` was added so project log files stay ignored unless deliberately tracked.

## Still optional/missing entries

- `Builds/`, `Recordings/`, `.utmp/`, `.gradle/`, and common generated binary/debug artifacts from the standard GitHub Unity template are still absent.
- `.serena/` is ignored only via `.git/info/exclude`, not project `.gitignore`; keep that local unless project wants to codify Serena metadata as ignored.

## Tracked generated state to handle separately

- `.vs/penrose_simulator/...` files are tracked already. Adding/fixing ignore patterns will not untrack them; removing them from the index would require explicit approval because it is a tracked-file change.

## Recommended direction

If more cleanup is desired, continue moving `.gitignore` toward the standard Unity template while preserving project-specific behavior:

- consider `/[Bb]uilds/`, `/[Rr]ecordings/`, `.utmp/`, `.gradle/`, `*.tmp`, `*.booproj`, `*.svd`, `*.pdb`, `*.mdb`, `*.opendb`, `*.VC.db`, `sysinfo.txt`, and `mono_crash.*`;
- do not ignore `Packages/packages-lock.json` or `ProjectSettings/*.asset`; those are useful Unity project state.
